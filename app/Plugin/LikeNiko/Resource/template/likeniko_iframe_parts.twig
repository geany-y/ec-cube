<iframe id="iframe-wrap" src="{{ info.node_server_address }}" name="niko" width="{{ info.target_img_width }}" height="{{ info.target_img_height }}" frameborder="0" scrolling="no">
    この部分はインラインフレームを使用しています。
</iframe> 
